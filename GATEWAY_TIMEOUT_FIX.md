# Исправление 504 Gateway Timeout

## Проблема

При обращении к сервису со стороны фронтенда возникали ошибки `504 Gateway Timeout`:

```
GET https://sticker-art-e13nst.amvera.io/stickers/CAACAgQAAxUAAWkVkq0lK0GFy3B3Grfpcx9cIRNrAAIEEwACrk2BUIjFMOVe2AoBNgQ?file=true 504 (Gateway Timeout)
```

### Причины

504 Gateway Timeout возникает, когда время обработки запроса превышает timeout, установленный на уровне gateway/proxy (обычно 30-60 секунд).

**Основные причины задержек:**

1. **Запросы к Telegram API** - могут занимать до 30 секунд + retry логика (до 3 попыток) = до 90+ секунд
2. **Конвертация TGS файлов** - может занимать до 30 секунд для больших файлов
3. **Операции с Redis/Disk cache** - могут быть медленными при большом объеме данных
4. **Отсутствие общего timeout** - запрос мог выполняться неограниченно долго

## Решение

### 1. Общий Timeout на Endpoint

Добавлен общий timeout на уровне endpoint (`ENDPOINT_TIMEOUT_SEC = 50` секунд), который меньше gateway timeout (обычно 60 секунд), чтобы приложение успевало вернуть ошибку до того, как gateway закроет соединение.

**Преимущества:**
- Защита от gateway timeout
- Возможность вернуть понятное сообщение об ошибке вместо 504
- Контроль времени выполнения запросов

### 2. Улучшенное Логирование

Добавлено детальное логирование времени выполнения каждой операции:

- **Redis cache** - время проверки кэша
- **Disk cache** - время проверки дискового кэша
- **Telegram API** - время запроса к API
- **Общее время** - полное время выполнения запроса

**Примеры логов:**

```
📦 CACHE: Redis HIT - file_id=XXX, redis_time=5ms, total_time=8ms
📦 CACHE: Disk HIT - file_id=XXX, disk_time=120ms, total_time=130ms
📥 CACHE: MISS - Telegram API fetch completed: telegram_time=2500ms, total_time=2650ms
WARNING - Slow Telegram API fetch for XXX: telegram_time=15000ms, total_time=15200ms
ERROR - Request timeout for sticker XXX after 50100ms (timeout limit: 50s)
```

### 3. Обработка Timeout Ошибок

Улучшена обработка timeout ошибок:
- Возвращается HTTP 504 с понятным сообщением
- Логируется время выполнения до timeout
- Предоставляется контекст ошибки

## Конфигурация

### Переменные окружения

```env
# Общий timeout для endpoint (должен быть < gateway timeout)
ENDPOINT_TIMEOUT_SEC=50

# Timeout для запросов к Telegram API
TELEGRAM_TIMEOUT_SEC=30

# Timeout для конвертации файлов
CONVERSION_TIMEOUT_SEC=30
```

### Рекомендации

1. **ENDPOINT_TIMEOUT_SEC** должен быть на 5-10 секунд меньше, чем gateway timeout
   - Gateway timeout обычно: 60 секунд
   - Рекомендуемое значение: 50 секунд

2. **TELEGRAM_TIMEOUT_SEC** должен быть достаточно большим для медленных запросов, но не слишком большим
   - Рекомендуемое значение: 30 секунд

3. **Мониторинг производительности** - используйте логи для отслеживания медленных операций:
   ```bash
   # Найти медленные запросы (>5 секунд)
   docker logs <container> 2>&1 | grep "Slow request"
   
   # Найти timeout ошибки
   docker logs <container> 2>&1 | grep "Request timeout"
   
   # Найти медленные Telegram API запросы (>10 секунд)
   docker logs <container> 2>&1 | grep "Slow Telegram API"
   ```

## Улучшения Производительности

### 1. Кэширование

Убедитесь, что кэширование работает правильно:
- **Redis cache** - для часто запрашиваемых файлов
- **Disk cache** - для долгосрочного хранения
- **HTTP Cache headers** - для кэширования на стороне клиента

### 2. Мониторинг

Отслеживайте метрики производительности:
- Время ответа по типам операций
- Процент cache hits
- Количество timeout ошибок
- Медленные операции (>5 секунд)

### 3. Оптимизация

Если проблема сохраняется:

1. **Увеличьте timeout** (если gateway позволяет):
   ```env
   ENDPOINT_TIMEOUT_SEC=55
   ```

2. **Проверьте производительность кэша**:
   - Redis может быть медленным при большом количестве ключей
   - Disk cache может быть медленным при большой нагрузке на диск

3. **Оптимизируйте запросы к Telegram API**:
   - Увеличьте `TELEGRAM_MAX_CONCURRENT_REQUESTS`
   - Уменьшите `TELEGRAM_REQUEST_DELAY_MS` (если нет rate limiting)

4. **Рассмотрите использование background задач** для долгих операций (загрузка новых файлов)

## Тестирование

После внесения изменений:

1. Проверьте логи на наличие timeout ошибок
2. Мониторьте время выполнения запросов
3. Убедитесь, что cache работает правильно
4. Тестируйте с новыми файлами (cache miss)

## Связанные Документы

- `PERFORMANCE_OPTIMIZATION.md` - общие рекомендации по оптимизации
- `TELEGRAM_API_LOGGING.md` - логирование Telegram API
- `LOGGING_IMPROVEMENTS.md` - улучшения логирования

