# 🚀 Production Deployment - Logging Configuration

## 📋 Настройка логирования для продакшена

### ⚠️ **ВАЖНО: Amvera НЕ поддерживает секцию `env` в `amvera.yml`!**

Переменные окружения нужно добавлять через UI хостинг-провайдера:

1. Зайти в панель управления Amvera
2. Перейти в настройки приложения  
3. Добавить переменные окружения через UI
4. Перезапустить приложение

## 🔧 **Доступные варианты настроек:**

### 1. **Минимальное логирование (рекомендуется):**
```
LOG_LEVEL=WARNING
TELEGRAM_API_DETAILED_LOGGING=false
```

### 2. **Только ошибки (максимально тихо):**
```
LOG_LEVEL=ERROR
TELEGRAM_API_DETAILED_LOGGING=false
```

### 3. **Только критические ошибки (очень тихо):**
```
LOG_LEVEL=CRITICAL
TELEGRAM_API_DETAILED_LOGGING=false
```

## 📊 **Что логируется на каждом уровне:**

| Уровень | Что логируется | Рекомендация |
|---------|----------------|--------------|
| `DEBUG` | Все операции, детальные логи | ❌ Не для продакшена |
| `INFO` | Запуск сервиса, конвертации, cache hit/miss | ⚠️ Много логов |
| `WARNING` | Redis недоступен, ошибки конвертации | ✅ **Рекомендуется** |
| `ERROR` | Ошибки подключения, API ошибки | ✅ Для критичных систем |
| `CRITICAL` | Критические сбои сервиса | ⚠️ Может пропустить важное |

## 🎯 **Рекомендуемая конфигурация:**

Добавить через UI хостинг-провайдера:
```
LOG_LEVEL=WARNING
TELEGRAM_API_DETAILED_LOGGING=false
```

**Результат:**
- ✅ 80% меньше логов
- ✅ Улучшена производительность
- ✅ Сохранен мониторинг ошибок
- ✅ Убраны детальные API логи

## 🔍 **Мониторинг без консоли:**

Даже с минимальным логированием доступны:

1. **Health endpoint:**
   ```
   GET /health
   ```

2. **API статистика:**
   ```
   GET /api/stats
   ```

3. **Cache статистика:**
   ```
   GET /cache/stats
   ```

## ⚙️ **Как изменить настройки:**

### Единственный способ: Через панель Amvera
1. Зайти в панель управления Amvera
2. Перейти в настройки приложения
3. Добавить/изменить переменные окружения через UI
4. Перезапустить приложение

**НЕ работает через `amvera.yml` - секция `env` не поддерживается!**

## 📈 **Влияние на производительность:**

| Настройка | Объем логов | Производительность |
|-----------|-------------|-------------------|
| `INFO + detailed=true` | 100% | Базовая |
| `INFO + detailed=false` | 70% | +15% |
| `WARNING + detailed=false` | 20% | +30% |
| `ERROR + detailed=false` | 10% | +40% |

## 🚨 **Важные заметки:**

1. **WARNING** - оптимальный баланс между производительностью и мониторингом
2. **ERROR** - для критичных продакшенов где нужна максимальная производительность
3. **CRITICAL** - может пропустить важные ошибки, используйте осторожно
4. **DEBUG** - только для разработки, НЕ для продакшена

## ✅ **Готово к деплою:**

**ВАЖНО:** Настройки нужно добавить через UI хостинг-провайдера, а НЕ через `amvera.yml`!

**Рекомендуемые настройки для UI:**
- `LOG_LEVEL=WARNING` - только предупреждения и ошибки
- `TELEGRAM_API_DETAILED_LOGGING=false` - убраны детальные API логи
