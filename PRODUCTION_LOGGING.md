# 📝 Production Logging Configuration

## 🎯 Рекомендуемые настройки логирования для продакшена

### Минимальное логирование (рекомендуется):
```bash
LOG_LEVEL=WARNING
TELEGRAM_API_DETAILED_LOGGING=false
```

### Только ошибки (максимально тихо):
```bash
LOG_LEVEL=ERROR
TELEGRAM_API_DETAILED_LOGGING=false
```

### Только критические ошибки (очень тихо):
```bash
LOG_LEVEL=CRITICAL
TELEGRAM_API_DETAILED_LOGGING=false
```

## 📊 Что логируется на каждом уровне:

### DEBUG (не рекомендуется для продакшена):
- Все операции Redis
- Детальные логи конвертации
- Каждый HTTP запрос
- Внутренние состояния сервисов

### INFO (по умолчанию):
- Запуск/остановка сервиса
- Подключение к Redis
- Конвертация файлов
- Cache hit/miss
- Rate limiting

### WARNING (рекомендуется для продакшена):
- Redis недоступен
- Конвертация не удалась
- Превышен размер файла
- Rate limit превышен

### ERROR:
- Ошибки подключения к Redis
- Ошибки Telegram API
- Ошибки конвертации
- Критические ошибки сервиса

### CRITICAL:
- Только самые критичные ошибки
- Сервис не может запуститься
- Критические сбои

## 🔧 Настройка через переменные окружения

### В .env файле:
```bash
# Минимальное логирование
LOG_LEVEL=WARNING
TELEGRAM_API_DETAILED_LOGGING=false
```

### В Docker:
```yaml
environment:
  - LOG_LEVEL=WARNING
  - TELEGRAM_API_DETAILED_LOGGING=false
```

### В командной строке:
```bash
export LOG_LEVEL=WARNING
export TELEGRAM_API_DETAILED_LOGGING=false
```

## 📈 Влияние на производительность

### TELEGRAM_API_DETAILED_LOGGING=false:
- ✅ Убирает детальные логи каждого API запроса
- ✅ Уменьшает I/O операции
- ✅ Снижает нагрузку на диск
- ✅ Ускоряет обработку запросов

### LOG_LEVEL=WARNING:
- ✅ Убирает INFO логи (cache hit/miss, конвертации)
- ✅ Оставляет важные предупреждения
- ✅ Значительно уменьшает объем логов

## 🎯 Рекомендуемая конфигурация для продакшена:

```bash
# Продакшен настройки
LOG_LEVEL=WARNING
TELEGRAM_API_DETAILED_LOGGING=false

# Остальные настройки
REDIS_HOST=redis-e13nst.db-msk0.amvera.tech
REDIS_SSL_ENABLED=true
WORKERS=4
RATE_LIMIT_ENABLED=true
```

## 🔍 Мониторинг

Даже с минимальным логированием вы все еще можете:

1. **Проверить health endpoint:**
   ```bash
   curl http://localhost:8081/health
   ```

2. **Посмотреть статистику API:**
   ```bash
   curl http://localhost:8081/api/stats
   ```

3. **Проверить статистику кеша:**
   ```bash
   curl http://localhost:8081/cache/stats
   ```

## ⚠️ Важные заметки:

1. **Не устанавливайте LOG_LEVEL=CRITICAL** - вы можете пропустить важные ошибки
2. **WARNING - оптимальный баланс** между производительностью и мониторингом
3. **ERROR - для критичных продакшенов** где нужна максимальная производительность
4. **DEBUG - только для разработки** и отладки

## 🚀 Быстрая настройка:

```bash
# Экспорт переменных
export LOG_LEVEL=WARNING
export TELEGRAM_API_DETAILED_LOGGING=false

# Или добавьте в .env файл
echo "LOG_LEVEL=WARNING" >> .env
echo "TELEGRAM_API_DETAILED_LOGGING=false" >> .env
```

## 📊 Сравнение объемов логов:

| Настройка | Объем логов | Производительность | Мониторинг |
|-----------|-------------|-------------------|------------|
| DEBUG + detailed=true | 100% | Медленно | Полный |
| INFO + detailed=true | 70% | Средне | Хороший |
| INFO + detailed=false | 40% | Быстро | Хороший |
| WARNING + detailed=false | 20% | Быстро | Базовый |
| ERROR + detailed=false | 10% | Очень быстро | Минимальный |

**Рекомендация:** `WARNING + detailed=false` для продакшена.
